// Cross-module events from artwork/. Empty in Phase 3c: dispatches sync + writes
// via CatalogService. Future events (artwork.resolved) added when needed.
export const ARTWORK_EVENTS = {} as const;
