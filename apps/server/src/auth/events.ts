/**
 * Cross-module events emitted by `auth/`. Empty in Phase 3b — auth is a provider only;
 * future events (e.g. `auth.session.created`) go here once a downstream module needs them.
 */
export const AUTH_EVENTS = {} as const;
