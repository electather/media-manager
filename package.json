{
  "name": "nama",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/plugins/*"
  ],
  "scripts": {
    "dev": "concurrently -k -n server,client -c blue,green \"bun run dev:server\" \"bun run dev:client\"",
    "dev:server": "bun --env-file=../../.env.dev --cwd apps/server --hot src/index.ts",
    "dev:client": "bun run --cwd apps/client dev",
    "build:client": "bun run --cwd apps/client build",
    "db:migrate": "bun --env-file=../../.env.dev --cwd apps/server src/db/migrate.ts",
    "check:file-sizes": "bun run tools/check-file-sizes.ts",
    "typecheck": "bun run --cwd packages/shared tsc --noEmit && bun run --cwd apps/server tsc --noEmit && bun run --cwd apps/client tsc --noEmit",
    "inspect": "bun x @modelcontextprotocol/inspector ",
    "version:packages": "vp dlx changeset version && vp install",
    "prepare": "vp config"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "concurrently": "^10.0.3",
    "fallow": "^2.103.0",
    "react-doctor": "^0.5.8",
    "vite-plus": "^0.2.1"
  },
  "overrides": {
    "vite": "catalog:",
    "vitest": "catalog:"
  },
  "packageManager": "bun@1.3.12",
  "catalog": {
    "es-toolkit": "^1.49.0",
    "vite": "npm:@voidzero-dev/vite-plus-core@latest",
    "vitest": "^4.1.9",
    "vite-plus": "latest",
    "@better-auth/oauth-provider": "^1.6.23",
    "better-auth": "^1.6.23",
    "hono": "^4.12.27",
    "jszip": "^3.10.1",
    "typescript": "^6.0.0",
    "ua-parser-js": "^2.0.5",
    "zod": "^4.3.6"
  }
}
