/**
 * Wire types for the first-install bootstrap flow.
 * Only public shape needed is claim body, derived from `bootstrapClaimSchema`
 * in `./schemas` and re-exported through the barrel.
 */
export {};
