/**
 * Wire types for the first-install bootstrap flow.
 *
 * The only public shape currently needed is the claim body, which is derived
 * from `bootstrapClaimSchema` in `./schemas` and re-exported through the barrel.
 */
export {};
