---
"@nama/server": minor
"@nama/client": minor
---

Removed the per-bucket count pips from the watchlist filter, which now uses plain navigable bucket tabs, and dropped the backing media counts endpoint that no longer had a consumer.
