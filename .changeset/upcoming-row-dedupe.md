---
"@ent-mcp/server": patch
---

Fixed the upcoming row showing the same show multiple times when several queued episodes shared a calendar entry, which produced duplicate React keys and broke the home feed.
