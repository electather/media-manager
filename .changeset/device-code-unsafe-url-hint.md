---
"@nama/client": patch
---

Added a host hint in the device-code panel when the verification URL is rejected as unsafe, so the panel no longer appears broken if a future plugin returns a non-https URL.
