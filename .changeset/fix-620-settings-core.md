---
"@nama/client": patch
---

Fixed duplicate public-config cache entries across settings tabs, corrected dirty-bar re-registration ordering, removed dead query key, localized remaining hard-coded English strings in sessions and catalog views, improved partial revoke feedback, added email format validation before change confirmation, and persisted email verification cooldown across page reloads.
