---
"@ent-mcp/server": patch
---

Demo notification job now lets admins pick the event type instead of the category, and builds a payload that matches the chosen type so the rendered notification reflects the selection.
