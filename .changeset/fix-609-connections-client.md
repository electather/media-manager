---
"@nama/client": patch
---

Fixed the connection setup form so a successful test no longer stays marked verified after you change a field, and hardened OAuth sign-in against unsafe redirect links.
