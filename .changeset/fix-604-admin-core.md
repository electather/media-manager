---
"@nama/client": patch
---

Fixed the all-permissions badge in the roles list to correctly identify any role with wildcard permissions, and corrected whitespace-only header values being accepted silently in the plugin security settings.
