---
"@ent-mcp/client": patch
---

Polished the home page so the hero halo blends naturally, progress bars sit at the edge of the artwork instead of breaking the image, the docked top nav stays readable on light backdrops, the bottom nav scales gracefully on small screens, and content rows align cleanly with the page grid.
