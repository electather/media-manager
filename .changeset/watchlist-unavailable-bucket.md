---
"@ent-mcp/client": minor
---

Added an "Unavailable" filter for watchlisted items that aren't on a connected media server. Sub-pages now show a content-shaped loading state and a clearer empty state explaining why a section is empty, and the active filter chip survives changing the sort order.
