---
"@ent-mcp/server": minor
---

Hide home rows that have no content for the current user, so an installed plugin with an empty feed (e.g. an empty watchlist) no longer ships an empty row to the client.
