---
"@nama/client": minor
---

The TMDB onboarding step is now optional when a bundled default key is available, and the shared credentials list shows the bundled default as a read-only entry your own key overrides.
