---
---

Refactor `/settings` into nested routes — each tab is now a deep-linkable URL (`/settings/profile`, `/settings/security`, `/settings/apps`, `/settings/danger`). Pure structural change with no user-visible behaviour difference.
