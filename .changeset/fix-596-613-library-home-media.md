---
"@nama/client": patch
"@nama/server": patch
---

Library filters now narrow by every selected value, ordered quality options by fidelity, and showed a clear message instead of raw diagnostic text when a view failed to load. Hardened the title details endpoint against arbitrary lookups.
