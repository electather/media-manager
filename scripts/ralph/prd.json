{
  "project": "media-manager",
  "branchName": "ralph/media-resource-unification",
  "description": "Media Resource Unification (epic #491, frontend half) - unify the media HTTP surface and its client mirror. Server: one /api/media/* adapter (a generic registry-dispatched source resolver GET /api/media/sources/:sourceId plus title/counts/moods/writes endpoints) composing the EXISTING home/watchlist/media module barrels. Wire: extend @ent-mcp/shared/media with the MEDIA_SOURCE_IDS as-const tuple, the relocated cursor codec, per-source param schemas, and the canonical Page shape. Client: one shared/media/ layer (one list-hook core, one query-key root, one MediaApiError, shared card/skeleton primitives); home and watchlist become thin shells. Plus adjacent tracks: watchlist route loaders (#513), Paraglide ICU variants (#511/#512), all-items virtual scroll (#519), correctness nits (#514/#515/#516). SOURCE OF TRUTH: docs/2026-05-30-media-resource-unification-design.md - read it before each story; the design wins on any conflict. Section refs (A1, B2, ...) and invariant/risk tags (V.RG1, V.A1, V.CU1, RISK-205, ...) point into that design. Extends the shipped docs/2026-05-26-media-pipeline-consolidation-design.md (commit dacd3825) which unified the internal read path; this work unifies the HTTP surface and its client mirror. NO server module re-org: no composition logic moves between home/watchlist/media (invariant V.A1); media never imports concrete sources (invariant V.RG1) - the source registry lives in the api/ adapter, composed from consumer barrels. The URL reflects the resource; the owning module reflects the logic; the adapter bridges them. Additive-first/subtractive-last: Part A (US-001..US-005) adds /api/media/* WITHOUT removing the old endpoints; Part B (US-006..US-012) migrates the client; the old surface is deleted ONLY in the cutover (US-013) - this is sequencing, NOT a compat shim (no translation layer, no dual maintenance). Use Vite+ only: `vp check`, `vp test`, `vp install` (never pnpm/npm/yarn/vitest/oxlint directly); `vp dlx` for one-off binaries. CHANGESET RULE: @ent-mcp/shared is internal-only and is NEVER listed in a changeset - its changes ride the consumer bump. User-facing minor bumps land at US-010 (@ent-mcp/client), US-012 (@ent-mcp/client), and US-013 (@ent-mcp/client + @ent-mcp/server); every other story uses an empty-frontmatter changeset (---\\n---). Parity is the success bar: capture fixtures from the LIVE old endpoints BEFORE US-003/US-004/US-005 and assert the new surface matches item ids/order/cursor - no user-visible behavior change except what the adjacent tracks (#511-#519) explicitly specify. Every Part B story edits apps/client/ React: invoke the relevant frontend skill before editing (frontend-feature-architecture, vercel-react-best-practices, vercel-composition-patterns, paraglide-js, vercel-react-view-transitions). Compact context at each phase boundary.",
  "userStories": [
    {
      "id": "US-001",
      "title": "Extend @ent-mcp/shared/media with the wire contract",
      "description": "As a developer, I want one shared media wire contract - source-id set, cursor codec, param schemas, and page shape - so that client and server agree on exactly one definition of each (A5).",
      "acceptanceCriteria": [
        "@ent-mcp/shared/media (existing subpath at packages/shared/src/media/) gains a MEDIA_SOURCE_IDS as-const tuple plus the derived MediaSourceId type. Values lift the existing slugs: recommendedForYou-tv, recommendedForYou-movies, continueWatching-active, continueWatching-next, becauseYouWatched, similarTo, yourWatchlist, upcomingForYou, trendingNow, newReleases, watchlist-items, watchlist-mood-items, watchlist-tonight, watchlist-recently. Watchlist buckets ride watchlist-items via a `bucket` param, NOT per-bucket ids.",
        "New exports land alongside the existing recommendation-engine MediaItem / ActiveRow / RowSort / RowFilter - names do NOT collide and MediaItem is NOT touched (consolidation D: MediaItem is already taken, do not reuse it).",
        "The cursor codec is MOVED here: the Cursor type, encode, decode(raw, expectedMode?), and the encodeSeedCursor helper. decode returns Cursor | null and never throws (invariant V.CU1).",
        "decodeSeedToken and SeedToken (home's similar-feed offset paging) STAY home-source-private - they are NOT relocated (they would drag home paging across the boundary).",
        "The server media barrel re-exports the relocated codec so server-internal consumers that import encode / Cursor from the media barrel today (e.g. home/sources/similar-paged.ts) keep their barrel import unchanged (invariant V.RG1 untouched). media/cursor.ts becomes a thin re-export of the shared codec.",
        "Per-source param zod schemas exist (discriminated per source): watchlist-items ({ bucket?, sort, mood?, limit, cursor? }), watchlist-mood-items ({ moodId, limit, cursor? }), seeded home ({ seedId, seedType } for similarTo), bounded ({ limit }), and void. Reuse the shapes of today's itemsQuerySchema / moodItemsQuerySchema.",
        "Page = { items: CompactMediaItem[]; cursor: string | null; partial: boolean } is exported as the canonical page shape.",
        "CompactMediaItem is re-exported from @ent-mcp/shared/media (its definition stays in @ent-mcp/shared/home to avoid a churn cascade); MediaDetailsResponse, SeasonAvailabilityResponse, WatchlistCounts, and WatchlistMoodSummary are likewise re-exported via the media subpath for one coherent client import surface.",
        "The shared module stays isomorphic and zod-only (no drizzle-orm / hono / framework deps - shared-package rule).",
        "Codec unit tests assert decode -> null on bad / foreign / mode-mismatched input for BOTH modes, and that encodeSeedCursor / decodeSeedToken round-trip.",
        "Changeset: empty frontmatter (@ent-mcp/shared is internal-only and never listed; no server behavior changes yet).",
        "`vp check` passes.",
        "`vp test` passes."
      ],
      "priority": 1,
      "passes": true,
      "notes": "Done. packages/shared/src/media/: MEDIA_SOURCE_IDS tuple + MediaSourceId (enums.ts); isomorphic cursor codec Cursor/encode/decode/encodeSeedCursor (cursor.ts, btoa/atob - no Buffer); per-source param schemas reusing itemsQuerySchema/moodItemsQuerySchema (source-params.ts); canonical Page + CompactMediaItem re-export (page.ts); MediaDetailsResponse/SeasonAvailabilityResponse/WatchlistCounts/WatchlistMoodSummary re-exports (index.ts). Server: media/cursor.ts now a thin re-export of shared; media/types.ts re-exports Page; media barrel exports encodeSeedCursor; similar-paged.ts re-exports encodeSeedCursor (decodeSeedToken/SeedToken stay home-private). 17 shared codec tests + full vp test (2454) green; vp check 0 errors. CONFLICT noted: design A1 says 'minor @ent-mcp/shared' but PRD AC + project CLAUDE.md say shared is internal-only never listed -> empty-frontmatter changeset used."
    },
    {
      "id": "US-002",
      "title": "Consumer source-registration maps via barrels",
      "description": "As a backend developer, I want each consumer to surface its sources as a registration map through its barrel so that the adapter can build one registry without media ever importing concrete sources (A4, invariant V.RG1).",
      "acceptanceCriteria": [
        "A MediaSourceRegistration<P> interface exists (adapter-visible) with fields: sourceId: MediaSourceId, rateLimit: 'read' | 'write' | undefined, paramSchema: ZodType<P>, cursorMode: 'keyset' | 'offset', cursorOnNull: '400' | 'firstPage', requiresInitialCursor?: boolean, eligibility?(ctx): Promise<boolean>, and build(ctx, params, cursor): { source; cfg; enrichRows? }.",
        "home/index.ts exports homeMediaSources - a THIN lift wrapping the existing ROW_PROVIDERS (which already carries public eligibility / initialCursor / load / cursorMode / requiresInitialCursor per home/internal/types.ts); the build thunk wraps today's row-page load. No home composition logic moves (invariant V.A1).",
        "watchlist/index.ts exports watchlistMediaSources - a NEW barrel export re-packaging the per-section source + cfg wiring (itemsSource / moodItemsSource / tonightSource / recentlySource and their toItemsParams / itemsCfg / moodItemsCfg / recentlyCfg) currently PRIVATE to watchlist/service.ts. The wiring stays in watchlist (invariant V.A1) - it is only surfaced via the barrel.",
        "cursorOnNull is set per consumer to preserve V.CU1 exactly: home sources '400', watchlist sources 'firstPage'.",
        "eligibility is carried on home registrations (mirrors today's composeRowPage 404-on-ineligible).",
        "No HTTP endpoint is added in this phase. A unit test asserts the registry builds and every MEDIA_SOURCE_IDS entry resolves to a registration whose build wires listRows with the right stages.",
        "Changeset: empty frontmatter (server-internal barrel additions; no public wire change yet).",
        "`vp check` passes.",
        "`vp test` passes."
      ],
      "priority": 2,
      "passes": true,
      "notes": "Done. Contract: apps/server/src/media/service/registration.ts defines MediaSourceRegistration<P, SP=P, Row=ActiveRow> (sourceId/rateLimit/paramSchema/cursorMode/cursorOnNull/requiresInitialCursor?/eligibility?/build) + BuiltMediaSource<SP,Row> + AnyMediaSourceRegistration (=<any,any,any> erased map element; P/SP/Row are invariant so any is the only sound erasure, mirrors Record<string,RowProvider>). Re-exported as type-only via media barrel (./service/* approved by boundaries test). Home: homeMediaSources (home/internal/media-sources.ts) is a THIN lift over ROW_PROVIDERS - each row gets a new RowProvider.buildPipeline(ctx,cursor) that returns {source,cfg,enrichRows}; loadRowPage now wired atop the shared buildRowPipeline so load + the registry path never drift (V.A1, logic stays home-side). All home rows route through makePipelineRow so all get buildPipeline. Home policy: paramSchema=voidParamsSchema (rows take only cursor; seed rides on cursor), rateLimit=undefined, cursorOnNull='400', eligibility from provider. Re-exported via service.ts->barrel. Watchlist: watchlistMediaSources (watchlist/internal/media-sources.ts) re-packages the service-private itemsSource/moodItemsSource/recentlySource/tonightSource + their cfg builders; rateLimit='read', cursorOnNull='firstPage', no eligibility. Re-exported via service.ts->barrel (watchlist not in boundaries ENABLED_MODULES so freer, but mirrored home's pattern). Test: api/procedures/__tests__/media-registry.test.ts (8 tests) composes {...home,...watchlist}, asserts all 14 MEDIA_SOURCE_IDS resolve (+no strays), per-consumer policy, requiresInitialCursor on becauseYouWatched/similarTo, home enrichRows present / watchlist absent, and watchlist-items keyset(recent)->offset(alpha/bucket). vp check 0 errors (1 pre-existing virtualizer warning), vp test 331 files/2462 tests green, fallow baseline unchanged. DESIGN GAPS FLAGGED FOR US-003: (1) watchlist-items cursorMode is param-dynamic (keyset recent / offset otherwise) - the static reg.cursorMode field can't express it; build().source.stages.cursorMode is authoritative. For watchlist (cursorOnNull firstPage) the resolver can decode the OUTER cursor WITHOUT expectedMode (inner decodeKeyset/paginateOffset are mode-robust + fall to first page) which is behaviorally identical to OLD readSection. Home (cursorOnNull 400) needs strict decode(raw, reg.cursorMode) since home rows are static. (2) watchlist-tonight via the generic resolver yields the FLAT enriched candidate page - the hero/alternate pick() + 5-min cache are envelope-side (tonight/section.ts) and NOT replicated by plain listRows; cutover must decide where the split lands (likely client-side per design B3)."
    },
    {
      "id": "US-003",
      "title": "Generic source resolver endpoint",
      "description": "As a client developer, I want one paginated read endpoint dispatched by source id so that every media list reads through a single path (A3).",
      "acceptanceCriteria": [
        "api/procedures/media.ts defines a REGISTRY: Record<MediaSourceId, MediaSourceRegistration> composed as { ...homeMediaSources, ...watchlistMediaSources }, and mounts GET /api/media/sources/:sourceId ADDITIVELY (old endpoints stay).",
        "The resolver is dumb dispatch (invariant V.MC1/V.PG1 - no enrich / sort / cursor logic in the resolver): look up the registration (unknown -> 404 media.source_unknown), apply reg.rateLimit, build the source context, run reg.eligibility if present (ineligible -> 404 media.source_ineligible), parse reg.paramSchema (invalid -> 400 http.invalid_input), decode the cursor, map null per reg.cursorOnNull (home '400' -> 400 media.cursor_invalid; watchlist 'firstPage' -> null), enforce requiresInitialCursor (missing -> 400 media.cursor_required), then build -> listRows(source, cfg, ctx, enrichRows) -> return the Page.",
        "Two-level cursor decode holds: the resolver decodes only the opaque OUTER Cursor; the source still parses its seed/keyset payload out of Cursor.k (decodeSeedToken in similar-paged, keyset decode in watchlist items) - inner-token parsing stays source-side, unchanged.",
        "Bounded sources (tonight / recently) return Page with cursor: null; today's WatchlistSectionResponse ({ items, partial }) gains the cursor field (pre-stable additive).",
        "requireSession is applied .use('*') on the /api/media router; sessionUserId(c) is read per handler.",
        "Resolver unit tests cover: unknown sourceId -> 404, ineligible -> 404, bad params -> 400, cursor-null mapping per cursorOnNull (home 400 / watchlist first-page), requiresInitialCursor -> 400, and rate-limit applied per registration.",
        "Read parity: for every list source, the resolver returns the same item ids / order / cursor as the corresponding old endpoint (/home/row, /watchlist/items, /watchlist/sections/*, /watchlist/moods/:moodId/items). Fixtures are captured from the LIVE old endpoints BEFORE this phase and asserted against the resolver (RISK-202).",
        "Changeset: empty frontmatter (additive surface not yet consumed; the user-facing bump lands at cutover US-013).",
        "`vp check` passes.",
        "`vp test` passes."
      ],
      "priority": 3,
      "passes": true,
      "notes": "Done. api/procedures/media.ts: REGISTRY={...homeMediaSources,...watchlistMediaSources} (typed Record<string,AnyMediaSourceRegistration|undefined>, looked up by string; V.RG1 holds - composed adapter-side); GET /api/media/sources/:sourceId mounted at /media in router.ts ADDITIVELY (home/watchlist mounts untouched). Resolver order per design A3: reg lookup (unknown->404 media.source_unknown) -> rateLimit (read->watchlistReadLimiter, write->watchlistWriteLimiter, imported from ./watchlist procedure - shared bucket, no double budget; instances RELOCATE to media.ts at cutover US-013) -> sessionUserId -> buildSourceContext -> eligibility (home only, .catch(()=>false)->404 media.source_ineligible) -> paramSchema.safeParse (fail->400 http.invalid_input) -> resolveCursor -> build -> listRows -> c.json(Page). requireSession .use('*') on /media router. buildSourceContext UNIONs both consumers' handles (statusBatch for home enrich-override; getArtwork+toCanonicalRow mirroring asWatchlistContext for watchlist default enrich); deadlineMs=Date.now()+8000 (home epoch semantics). CURSOR DECODE (US-002 design-gap resolution): home cursorOnNull '400' => STRICT decode(raw, reg.cursorMode) (static rows; undecodable->400 media.cursor_invalid; requiresInitialCursor missing->400 media.cursor_required); watchlist cursorOnNull 'firstPage' => LENIENT decode(raw) (no expectedMode) so the dynamic watchlist-items offset cursor survives - the built source's paginate stage falls a mode-mismatched cursor to first page, behaviorally identical to old readSection. '\"null\"'/empty/absent cursor = first page (matches old home). enrichRows present (home) -> 4-arg listRows overload; absent (watchlist) -> 3-arg default-fan-out overload. New shared error codes registered in packages/shared/src/diagnostics/codes.ts (media.source_unknown/source_ineligible/cursor_invalid/cursor_required, all severity info). Tests: media-resolver.test.ts (13 - fake regs + stub listRows: 401/unknown-404/ineligible-404/eligibility-throw-404/200-Page/enrichRows-arity/bad-params-400/home-cursor-400/watchlist-firstPage/requiresInitialCursor-400/valid-cursor-threaded/limiter-per-reg/429-short-circuit) + media-parity.test.ts (8 - real registries + spied listRows, asserts source.stages+cfg identical between old service fns and resolver HTTP for items keyset+keyset-page2+alpha-offset-page2+bucket, mood, recently, bounded Page-shape, and home build===provider.buildPipeline). vp check 0 errors (1 pre-existing virtualizer warning), vp test 333 files/2483 tests green, fallow baseline unchanged. Changeset empty-frontmatter (additive surface not consumed until cutover US-013). FLAGGED: watchlist-tonight via resolver yields the FLAT enriched candidate page (cursor:null) - hero/alternate pick()+5min cache stay envelope-side (tonight/section.ts), NOT replicated by listRows (design A3/B3 keeps the split client-side); tonight parity is therefore on the flat candidate set, not old /sections/tonight shape. Also: watchlist deadlineMs changes from 5000(epoch->always-past, retries disabled) to now+8000(retries enabled) via the unified ctx - only affects partial flags under plugin failure, not item ids/order/cursor; old /watchlist/* endpoints keep old behavior until cutover."
    },
    {
      "id": "US-004",
      "title": "Title resource endpoints",
      "description": "As a client developer, I want a media title's details and availability under the media URL namespace so that the detail view reads from one resource surface (A2, A6).",
      "acceptanceCriteria": [
        "GET /api/media/:type/:tmdbId/details is mounted and bridges to home.composeDetails(ctx, tmdbId, type) (the existing composer - its payload already carries seasons metadata inside MediaDetailsExtra). Response type is the unchanged MediaDetailsResponse.",
        "GET /api/media/:type/:tmdbId/availability is mounted and bridges to home.composeSeasonAvailability(ctx, tmdbId). Response type is the unchanged SeasonAvailabilityResponse.",
        ":type in {movie, tv} is a PATH param (today's /home/details?tmdbId&mediaType becomes /api/media/:type/:tmdbId/details).",
        "There is NO separate /seasons endpoint - seasons metadata rides inside details (no new composer; preserves invariant V.A1).",
        "The composition logic does NOT move out of home - the adapter is a one-line bridge per endpoint (RISK-203).",
        "Read parity: details and availability outputs match the old /home/details and /home/season-availability for the same inputs (fixtures captured before this phase).",
        "Changeset: empty frontmatter (additive; not yet consumed).",
        "`vp check` passes.",
        "`vp test` passes."
      ],
      "priority": 4,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-005",
      "title": "Counts, moods, and writes endpoints",
      "description": "As a client developer, I want watchlist counts, mood summary, and add/remove writes under the media namespace so that aggregates and mutations share the one surface (A6).",
      "acceptanceCriteria": [
        "GET /api/media/counts bridges to watchlist.getCounts(ctx) (returns the unchanged WatchlistCounts).",
        "GET /api/media/moods bridges to watchlist.getMoodSummary(ctx) (returns the unchanged WatchlistMoodSummary).",
        "POST /api/media/watchlist bridges to the media writes barrel addItem; returns AddWatchlistResponse (201 on insert, 200 when already active).",
        "DELETE /api/media/watchlist/:type/:tmdbId bridges to the media writes barrel removeItem; returns 204. :type / :tmdbId are path params.",
        "Rate limits are preserved per registration / endpoint policy: counts / moods -> watchlistReadLimiter; writes -> watchlistWriteLimiter; limiter instances move with the router, keys unchanged (A7).",
        "Derivation / tally ownership is unchanged - counts and mood summary stay computed where they are today; writes stay media-owned (since consolidation Phase 2).",
        "Parity: counts and mood-summary outputs match the old /counts and /moods for the same fixtures.",
        "Changeset: empty frontmatter (additive; not yet consumed).",
        "`vp check` passes.",
        "`vp test` passes."
      ],
      "priority": 5,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-006",
      "title": "Shared client media layer",
      "description": "As a frontend developer, I want one client media layer - source descriptor, list-hook core, query-key root, error class, cursor codec - so that home and watchlist stop maintaining two of each (B1, #504/#505/#509, invariant V.CL1).",
      "acceptanceCriteria": [
        "Invoke the frontend skill before editing apps/client/ (frontend-feature-architecture for layer structure; vercel-react-best-practices for hooks/data-fetch).",
        "apps/client/src/shared/media/source.ts defines ClientMediaSource<P> mirroring the server registration: { sourceId: MediaSourceId; params: P; mode: 'infinite' | 'section'; cursorOnNull: 'throw' | 'firstPage'; initialCursor?: string | null }, with fetchPage(params, cursor) binding the ONE endpoint api.media.sources[':sourceId'].$get({ param, query }) -> Page. No per-feature fetcher.",
        "apps/client/src/shared/media/use-media-rows.ts defines one core mediaRowsQueryOptions(source) (infinite query options: getNextPageParam: p => p.cursor ?? undefined, flatten pages -> items, OR-reduce partial) plus two THIN wrappers: useMediaRows (useSuspenseInfiniteQuery) and useMediaRowsLazy (useInfiniteQuery). The cursor / flatten / partial logic is defined exactly once.",
        "apps/client/src/shared/media/query-keys.ts defines a mediaKeys root and factory: mediaKeys.source(sourceId, params), mediaKeys.title(type, tmdbId), mediaKeys.counts(), mediaKeys.moods().",
        "apps/client/src/shared/media/error.ts defines one MediaApiError and one throwOnError.",
        "apps/client/src/shared/media/cursor.ts re-exports the @ent-mcp/shared/media codec; similarTo builds its initial cursor via encodeSeedCursor (closing the consolidation H client gap).",
        "Unit tests cover: mediaRowsQueryOptions flatten + getNextPageParam + partial OR-reduce; suspense vs lazy wrappers; the similarTo client-built cursor is accepted by the resolver.",
        "Changeset: empty frontmatter (new internal client layer; not yet wired into a visible surface).",
        "`vp check` passes.",
        "`vp test` passes."
      ],
      "priority": 6,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-007",
      "title": "Shared media primitives and optimistic mutation",
      "description": "As a frontend developer, I want one card, one skeleton, and one optimistic-mutation path so that both features render and mutate through shared primitives (B2, #504/#505/#514).",
      "acceptanceCriteria": [
        "Invoke the frontend skill before editing apps/client/ (vercel-composition-patterns for the shared card/skeleton API; vercel-react-best-practices for the mutation hook).",
        "apps/client/src/shared/components/media-row-card.tsx is the single card both features render - a variant prop selects rail (16/9) vs grid (2/3), with slots for the match-reason chip (home) and source/added metadata (watchlist). It composes the existing MediaCard* primitives.",
        "apps/client/src/shared/components/grid-skeleton.tsx is the single Suspense grid fallback (shape varies by prop). The existing shared/components/empty-state/ is composed by per-feature wrappers (e.g. WatchlistEmpty) as SIBLING components - no nested sub-folders (repo convention).",
        "Shared optimistic add/remove (over use-optimistic-array-mutation) invalidates mediaKeys.root ONCE (not per feature, #505). useWatchlistIdSet / useIsInWatchlist snapshots are scoped to the watchlist sub-key, not the whole cache (#514).",
        "Tests assert the one-shot mediaKeys.root invalidation on add/remove and that the id-set snapshot is scoped to the watchlist sub-key.",
        "Verify in browser using dev-browser skill: the shared card renders correctly in both the home rail (16/9) and the watchlist grid (2/3) variants, and the skeleton matches each.",
        "Changeset: empty frontmatter (shared primitives; behavior-neutral until features migrate).",
        "`vp check` passes.",
        "`vp test` passes."
      ],
      "priority": 7,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-008",
      "title": "Migrate watchlist to the shared layer",
      "description": "As a frontend developer, I want watchlist reading through the shared media layer and api.media.* so that its bespoke fetcher / error / key code is deleted while product chrome is preserved (B3, #506/#508/#509/#515/#516).",
      "acceptanceCriteria": [
        "Invoke the frontend skill before editing apps/client/ (frontend-feature-architecture for the shell migration).",
        "Watchlist registers ClientMediaSources: watchlist-items (with bucket param), watchlist-mood-items, watchlist-tonight, watchlist-recently - all reading via api.media.sources[':sourceId'].",
        "Deleted from watchlist: lib/fetchers.ts, the standalone query-key factory (now derived from mediaKeys), the WatchlistItem re-export (import CompactMediaItem directly, #508), and WatchlistApiError (use MediaApiError).",
        "Product chrome is preserved: bucket chips, sort select, mood mosaic/cluster, section headers, the peek modal, and the tonight hero/alternate split.",
        "Nits: an unknown moodId route param triggers throw notFound() (#515); the watchlist card guards clearLogo (#516).",
        "Section parity holds: items / mood-items / tonight / recently render the same item ids and order as before the migration (asserted against the pre-migration fixtures from US-003).",
        "Verify in browser using dev-browser skill: each watchlist section (items by bucket, mood cluster, tonight, recently) loads, paginates via load more, and the peek modal opens - visually identical to before.",
        "Changeset: empty frontmatter (parity migration; no user-visible change).",
        "`vp check` passes.",
        "`vp test` passes."
      ],
      "priority": 8,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-009",
      "title": "Migrate home to the shared layer",
      "description": "As a frontend developer, I want the home rows reading through the shared media layer so that home's bespoke fetcher / key code is deleted while hero / match-reason / layout are preserved (B3, #507).",
      "acceptanceCriteria": [
        "Invoke the frontend skill before editing apps/client/ (frontend-feature-architecture for the shell migration).",
        "Each /home/layout row stub (already carrying sourceId = rowId + initialCursor) feeds a useMediaRowsLazy source; rails render the shared media-row-card + grid-skeleton.",
        "Deleted from home: the lib/fetchers.ts row fetcher and the home query-key root (derived from mediaKeys).",
        "useHomeDetails reads via api.media[':type'][':tmdbId'].details.",
        "Preserved unchanged: the hero / top-zone, the match-reason copy map, layout ordering, and ambient treatment.",
        "Layout parity holds: rails render identical item ids / order, and there are NO extra fetches beyond the layout call + per-row lazy loads (assert against the home-layout fixtures).",
        "Verify in browser using dev-browser skill: the home feed renders all rows with per-row skeletons, hero and match-reason copy are intact, and the network panel shows one layout call plus one lazy fetch per visible row (no duplicate fetches).",
        "Changeset: empty frontmatter (parity migration; no user-visible change).",
        "`vp check` passes.",
        "`vp test` passes."
      ],
      "priority": 9,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-010",
      "title": "Watchlist route loaders",
      "description": "As a user, I want watchlist routes to prefetch their first page so that navigation shows content immediately instead of a spinner (B4, #513).",
      "acceptanceCriteria": [
        "Invoke the frontend skill before editing apps/client/ (frontend-feature-architecture for route loaders).",
        "Each of the watchlist routes gains a loader that calls ensureInfiniteQueryData(mediaRowsQueryOptions(source)) to prefetch the first page, plus an errorComponent (TanStack Router convention).",
        "Home is unchanged here - it already loader-prefetches its layout; rows stay in-component lazy.",
        "Tests assert the loader prefetches the first page (cache is warm on mount) and the errorComponent renders on a failed loader.",
        "Verify in browser using dev-browser skill: navigating to a watchlist section route shows content on first paint (no full-section spinner), and a forced fetch error renders the route's errorComponent.",
        "Changeset: minor @ent-mcp/client - user-facing (faster navigation; one short past-tense end-user sentence).",
        "`vp check` passes.",
        "`vp test` passes."
      ],
      "priority": 10,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-011",
      "title": "Paraglide ICU variants",
      "description": "As a maintainer, I want the watchlist and home labels expressed as keyed ICU variants so that the message count drops and translations stay consistent (B4, #511/#512).",
      "acceptanceCriteria": [
        "Invoke the paraglide-js skill before converting messages.",
        "Bucket / empty / chip-ARIA labels and section / cluster / mood / sort labels are converted to ICU variants keyed on {bucket} / {moodId} / {sortKey}.",
        "The message count on the watchlist + home surface drops by >= 30% versus before.",
        "No user-visible copy changes - each variant renders the same string as the message it replaced (assert a representative set).",
        "Verify in browser using dev-browser skill: bucket chips, empty states, and section/mood/sort labels render identical copy to before the variant conversion.",
        "Changeset: empty frontmatter (i18n refactor; no user-visible copy change).",
        "`vp check` passes.",
        "`vp test` passes."
      ],
      "priority": 11,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-012",
      "title": "All-items virtual scroll",
      "description": "As a user, I want the all-items watchlist views virtualized so that long lists stay smooth (B4, #519).",
      "acceptanceCriteria": [
        "Invoke the frontend skill before editing apps/client/ (vercel-react-best-practices for the virtualized list).",
        "The all-items load more lists (every bucket + mood route) render through useVirtualizer over useMediaRows, fetching the next page near the window end.",
        "Only the visible window (plus overscan) is in the DOM for a long list.",
        "Tests assert a page fetch fires when the scroll position nears the window end.",
        "Verify in browser using dev-browser skill: scrolling a long bucket / mood list keeps the DOM node count bounded and fetches the next page near the end, with no visual jank.",
        "Changeset: minor @ent-mcp/client - user-facing (smoother long lists; one short past-tense end-user sentence).",
        "`vp check` passes.",
        "`vp test` passes."
      ],
      "priority": 12,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-013",
      "title": "Delete the old surface and dead client code",
      "description": "As a maintainer, I want the old per-product endpoints and client shims removed so that api.media.* and the shared layer are the only media surface (A8, D cutover Z).",
      "acceptanceCriteria": [
        "Deleted (pre-stable, no shim): the /home/row route from api/procedures/home.ts, the /home/details and /home/season-availability routes, and ALL of api/procedures/watchlist.ts. /home/layout is KEPT (the home procedure is now layout-only).",
        "api/router.ts is re-pointed: mount mediaApp at /media; the home group keeps only /layout; the /watchlist mount is dropped. AppType regenerates so api.media.* is canonical and api.watchlist.* + api.home.{row,details,season-availability} no longer exist.",
        "Client dead code is deleted: any remaining home/watchlist fetcher / hook / type shims superseded by shared/media/.",
        "The .fallow/dead-code-baseline.json baseline does NOT grow (Rule 14); any intentional exception carries an inline // fallow-ignore-* with a one-line reason.",
        "A grep gate confirms no caller references the deleted endpoints, WatchlistItem, HomeApiError, or WatchlistApiError.",
        "Verify in browser using dev-browser skill: a full smoke pass - home feed, every watchlist section, the peek/detail view, and add/remove - works end-to-end against api.media.* only (network panel shows no /home/row, /watchlist/*, /home/details, or /home/season-availability calls).",
        "Changeset: minor @ent-mcp/client and @ent-mcp/server - user-facing (media data now served from one unified API; one short past-tense end-user sentence). Note cursor invalidation on deploy (RISK-206).",
        "`vp check` passes.",
        "`vp test` passes."
      ],
      "priority": 13,
      "passes": false,
      "notes": ""
    }
  ]
}
